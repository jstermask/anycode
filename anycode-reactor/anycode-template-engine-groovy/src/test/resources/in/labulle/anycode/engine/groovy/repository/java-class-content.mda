package ${c.owner.getFullyQualifiedName(".")};

${java.classifierSignature(c)} {
	<% c.attributes.each() { 
		println java.attribute(it) } %>

	<% c.attributes.each() { 
		println java.getter(it) 
		println java.setter(it)} %>
	
	<% c.operations.each() { 
		println java.operation(it) } %>
	
}